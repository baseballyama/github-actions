name: Release

on:
  push:
    branches:
      - master
      - develop
      - "releases/**"

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      uses: peter-evans/create-pull-request@v3.10.1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

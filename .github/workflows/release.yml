name: Release

on:
  push:
    branches:
      - master
      - develop
      - "releases/**"

jobs:
  release:
    name: Release
    uses: peter-evans/create-pull-request@v3.10.1
    with:
      token: ${{ secrets.GITHUB_TOKEN }}
